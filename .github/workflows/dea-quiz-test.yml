name: dea-quiz-tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # ======================
      # Prepare Phase
      # ======================
      - name: 'Prepare: Checkout repository'
        uses: actions/checkout@v4

      - name: 'Prepare: Setup Node.js runtime'
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: 'Prepare: Install npm dependencies'
        run: npm ci

      - name: 'Prepare: Install Playwright browser'
        run: npx playwright install --with-deps chromium

      # ======================
      # Quality Gates
      # ======================
      - name: 'Quality Gate: Check code formatting'
        run: npm run format:check

      - name: 'Quality Gate: Validate DEA question schema'
        run: npm run validate:dea-questions

      # ======================
      # E2E Coverage
      # ======================
      - name: 'E2E: Run DEA suites listed in docs/testing/e2e-test-catalog.md'
        run: |
          echo "Catalog: docs/testing/e2e-test-catalog.md"
          echo "Suite naming: [DEA][UI|FLOW|DATA] Feature / Screen"
          npx playwright test tests/e2e/dea-*.spec.ts --reporter=list

      # ======================
      # Reporting
      # ======================
      - name: 'Report: Upload Playwright artifacts'
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dea-playwright-artifacts
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
