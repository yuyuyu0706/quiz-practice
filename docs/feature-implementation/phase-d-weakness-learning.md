# Phase D：弱点学習 機能実装書

## この文書について

この文書は、DEPクイズアプリのPhase D「弱点学習」について、機能群が必要とされた背景、各機能の関係、設計思想、現在の到達点、今後の発展余地を理解するための機能実装書である。

Phase Dでは、学習履歴から弱点を見つけるだけでなく、分析結果を確認し、弱点に沿って復習し、必要に応じて学習履歴を安全に切り替えることで、次の学習判断につなげる循環を整備した。この文書は、その循環を初見の読者にも追えるように説明することを目的とする。

本Issueの範囲では、後続の目次設計・全体草案・章別詳細化の判断基準となる入口部分を定義する。最終目次や各章の詳細本文は、後続Issueで確定する。

### 目的

この文書の目的は、Phase D「弱点学習」が、なぜ必要とされ、どのような機能群として実装され、各機能がどのような学習循環を形成し、どの設計思想によって支えられているかを理解できるようにすることである。

読者がこの文書を読んだ後に、少なくとも次の流れを説明できる状態を目指す。

1. Phase Dが解決しようとした課題。
2. 弱点分析だけでは学習行動につながり切らなかった理由。
3. 対象問題一覧、弱点起点の復習、学習履歴リセットが必要になった理由。
4. 各機能がどのように連携して、分析から復習、再分析へ向かう学習フィードバックループを作るか。
5. 現行実装で重視した設計判断と、その背景にある考え方。
6. 将来、時系列分析・クロス分析・学習効果測定・AI推薦などへ発展させる際に、どの観点を引き継ぐべきか。

この文書は、Issue、PR、コード、Unit Test、E2E、E2Eカタログの内容を別形式で網羅的に再掲するものではない。詳細な要件、変更差分、正確な実装契約、保証される振る舞いは、それぞれの成果物を正本として参照する。

### 想定読者

#### 主読者

主読者は、将来Phase Dや弱点学習機能を改修する開発者である。特に、コードや個別Issueを読む前に、機能全体の意図、構造、設計判断の背景を把握したい開発者を想定する。

この読者に対しては、関数や画面を単独で説明するのではなく、学習履歴を次の学習判断に利用するために、なぜその機能が必要になったのかを中心に説明する。

#### 副読者

副読者は、DEPクイズアプリの機能体系やPhase Dの成果を把握したい関係者である。詳細Issueをすべて追わなくても、Phase Dがどのような価値を追加し、既存のクイズ学習体験をどのように拡張したのかを理解できることを重視する。

#### 非対象の読者像

この文書は、次の用途を主目的にしない。

- 操作手順だけを知りたい利用者向けマニュアル。
- API、関数、型、storage keyの完全な仕様だけを調べるコードリファレンス。
- 全PR差分や全Issue本文を追跡する変更履歴。
- Unit TestやE2Eケースを逐語的に列挙するテスト一覧。

### 文書の位置付け

Phase Dに関する成果物は複数に分かれているため、この文書では次の役割分担を前提にする。

| 成果物          | 主な役割                                                    |
| --------------- | ----------------------------------------------------------- |
| Issue           | 要件、背景、判断経緯、受入条件を記録する。                  |
| PR              | 実際に変更した内容とレビュー単位を記録する。                |
| コード          | 現行実装の正本として、実際の動作と契約を示す。              |
| Unit Test / E2E | 保証される振る舞いと回帰防止対象を示す。                    |
| E2Eカタログ     | E2Eテスト責務の正本として、何をどの粒度で確認するかを示す。 |
| 機能実装書      | 機能の意味、目的、関係、設計思想、現在地、展望を説明する。  |

機能実装書では、「どう実装したか」よりも「なぜそう実装したか」を優先する。ただし、設計思想を理解するために必要な範囲では、`depQuizProgress`、`depQuizSettings`、`depQuizActiveSession`、`buildWeaknessAnalysis()`、`weaknessReview`のような実装名を扱うことがある。

一方で、関数の全引数・全返却値、全プロパティ一覧、全Unit Test / E2Eケース、PRごとの差分、Issue本文の全文再掲は本文の中心にしない。これらは、必要に応じて各正本へ読者を誘導する。

### 記述方針

#### 目的から書く

各機能は、実装名や関数名から説明を始めない。まず、その機能が何を解決するために必要だったのかを説明する。

例えば、弱点分析は「`buildWeaknessAnalysis()`を追加した」ことではなく、「蓄積された学習履歴を次の学習判断に利用できるよう、全体・Section・誤答理由の観点から弱点を分析する機能を設けた」こととして説明する。

#### 学習循環の中で説明する

機能単体の追加事実ではなく、学習体験上の役割を中心に説明する。対象問題一覧であれば、単に一覧画面を追加したと説明するのではなく、分析結果を確認し、復習対象に納得し、弱点起点の復習へ進むための接続点として説明する。

この文書では、分析、対象確認、復習、再分析、必要に応じた履歴リセットがどのように結び付き、学習フィードバックループを形成するかを重視する。

#### 実装詳細は必要最小限にする

設計判断を理解するために必要な技術要素は扱うが、コード解説書にはしない。実装名、storage key、mode名などは、文脈を理解する補助として利用し、詳細な仕様確認はコードやテストに委ねる。

#### 現行実装と将来展望を分離する

現行実装、実装済みの事実、将来案を同じ表現レベルで混在させない。

現行実装として扱う内容には、全体・Section・誤答理由タグ別の分析、対象問題一覧、`weaknessReview`、学習履歴リセットなどが含まれる。将来展望として扱う内容には、時系列分析、クロス分析、学習効果測定、AI推薦などが含まれる。

将来展望を書く場合は、現時点で実装済みであるかのように読める表現を避け、現行機能から自然に発展し得る方向性として明確に分離する。

#### Issue番号を本文の主語にしない

本文では、「どのIssueで実装されたか」ではなく、「どの機能が何を解決するか」を主語にする。IssueやPRは、詳細確認のための参照先として扱う。

例えば、「#197で実装した」ではなく「弱点分析」、「#253で実装した」ではなく「学習履歴リセット」のように、機能や学習体験上の役割を中心に記述する。

#### 初見読者がIssue階層を知らなくても読めるようにする

本文は、Issue階層や過去の議論を知らない読者でも、Phase Dの主要な意味、機能関係、設計思想を理解できる自己完結性を持たせる。

必要な背景は本文中で要約し、詳細な判断経緯や受入条件を確認したい場合にIssueやPRを参照できる構成にする。

### 後続の目次設計で使う判断基準

後続の目次案比較では、次の観点を判断基準にする。

- 初見読者が理解しやすいか。
- Issue階層を知らなくても読めるか。
- 実装目的が中心になっているか。
- 学習フィードバックループが理解できるか。
- 詳細仕様の再掲になっていないか。
- 章間重複を避けられるか。
- 現行実装と将来展望を分離できるか。
- 今後の分析・可視化・学習支援の展望まで自然につながるか。
- 後続の章別詳細化へ分割しやすいか。

この判断基準は、後続Issueで最終目次や各章の責務を確定するための前提であり、本Issueでは最終目次そのものは確定しない。
